import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['dummyimage.com', 'avatar.iran.liara.run', 'i.pravatar.cc', 'images.unsplash.com'],
  },
  /* config options here */
};

export default nextConfig;
